extends layout


block content
 section#content 
   h2= prompt 
     if hint 
     p 
      i Hint: #{hint}     
   a(href = `${id}?side=${sideToShow}`)= sideToShowDisplay   
   br
   a(href='/cards') Next card